package de.croggle.game;

/**
 * Responsible for creating new event handlers.
 *
 */
public class EventHandlerFactory {

	/**
	 * Creates a new event handler which can process events of the param type.
	 * @param event is the type of event the handler has to process.
	 * @return a new event handler.
	 */
	public static AbstractEventHandler getEventHandler(Event event) {
		return null;
	}
}
