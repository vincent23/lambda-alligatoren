package de.croggle.controller.event;

/**
 * The base interface for all events that occur while a 
 * board controller (= simulator) performs changes on a board.
 * 
 */
public interface BoardEvent {
	
}
