package de.croggle.ui.screens;

/**
 * Screen, which is used for both creating a new account with a given name as well as changing the name of an existing account.
 * For reference see "Pflichtenheft 10.5.14 / Abbildung 23"
 */
public class ProfileSetAvatarScreen extends AbstractScreen{
	
}
